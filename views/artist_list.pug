extends layout

block content
  h1= title

  ul
    each artist in artist_list
      li
        a(href=artist.url) #{artist.name}
        |  (#{artist.debut_date_formatted})

    else
      li There are no artists.